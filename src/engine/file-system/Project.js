/*
TODO:
- Create a "Project" class
    - Location, files, creation
    - Versionning
    - Collections ? (let the possibility to organize in a different way than folders)
- View :
    - View all files by type, by collection
    - Add description signet on a file
    - Add bookmarks
    - Create scopes (include multiple files from differentes locations and view only these scoped files in a sub-view (phpstorm))
*/